import { DesignerProfileRequest } from './designer-profile-request';

describe('DesignerProfileRequest', () => {
  it('should create an instance', () => {
    expect(new DesignerProfileRequest()).toBeTruthy();
  });
});
