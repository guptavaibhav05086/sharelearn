import { PrinterProfileRequest } from './printer-profile-request';

describe('PrinterProfileRequest', () => {
  it('should create an instance', () => {
    expect(new PrinterProfileRequest()).toBeTruthy();
  });
});
